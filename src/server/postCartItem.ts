import { db } from "./db";
import { cartItems } from "./db/schema";
import { GetCart } from "./getCart";

export async function PostCartItem(itemId: bigint) {
  // const cart = await GetCart();
  // const cartItem = await db.insert({
  //   cart
  // })
}
